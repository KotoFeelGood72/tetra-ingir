if(devStatus) {
	pageWidget(['index']);
	pageWidget(['contacts']);
	pageWidget(['gallery']);
	pageWidget(['policy']);
	pageWidget(['services']);
	pageWidget(['single-services']);
	getAllClasses('html', '.elements_list');
}